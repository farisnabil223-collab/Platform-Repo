# @eduverse/ui

EduVerse Enterprise Component Library.

## Features
- Strictly typed and styled using Design Tokens.
- Built-in Dark Mode and RTL (Arabic) rendering.
- Reusable layouts, animated SVG charts, and forms.

## Usage
Import components from `@eduverse/ui`:
```tsx
import { Button, Input } from '@eduverse/ui';
```
