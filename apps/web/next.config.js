/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  transpilePackages: ['@eduverse/ui', '@eduverse/design-tokens'],
};

module.exports = nextConfig;
/** @type {import('next').NextConfig} */
